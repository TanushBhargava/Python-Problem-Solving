class Solution:
    def maxSubarraySum(self, arr):
        cs = 0
        ms = arr[0]

        for i in arr:
            cs += i
            ms = max(ms, cs)
            if cs < 0:
                cs = 0

        return ms
